services:
  - type: web
    name: turipaz
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn mi_proyecto.wsgi:application --bind 0.0.0.0:$PORT
```

#### 2. Verifica tu `requirements.txt`:

Asegúrate de que incluya al menos:
```
Django>=4.0
gunicorn>=20.1.0